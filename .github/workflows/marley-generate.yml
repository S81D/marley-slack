name: MARLEY generate

# Manual trigger only -- never on push. Slack reaches this through the GitHub
# REST API (see slack/SETUP.md), which is what workflow_dispatch responds to.
on:
  workflow_dispatch:
    inputs:
      invoker:
        description: "Who asked for the event (display only, never executed)"
        required: false
        default: "the watch"

# One neutrino at a time. Extra requests queue instead of piling up runners.
concurrency:
  group: marley-generate
  cancel-in-progress: false

env:
  # Pinned deliberately: MARLEY's main branch moves, and a moving target would
  # make runs non-reproducible and failures hard to attribute.
  MARLEY_REF: v2.0.0

permissions:
  contents: read

jobs:
  generate:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Check out this repository
        uses: actions/checkout@v4

      # Cheap and fails fast: the parser is checked against fixtures, one of
      # which is real MARLEY output, before any C++ compilation happens.
      - name: Check the event parser
        run: tests/run_tests.sh

      - name: Check out MARLEY
        uses: actions/checkout@v4
        with:
          repository: MARLEY-MC/marley
          ref: ${{ env.MARLEY_REF }}
          path: marley

      # GSL is an optional MARLEY dependency with a bundled fallback; using the
      # distro package is faster than compiling the bundled copy.
      - name: Install GSL
        run: |
          sudo apt-get update
          sudo apt-get install -y libgsl-dev

      - name: Cache the MARLEY build tree
        uses: actions/cache@v4
        with:
          path: marley/build
          key: marley-${{ env.MARLEY_REF }}-${{ runner.os }}-gsl

      - name: Build MARLEY and generate one event
        env:
          SEED: ${{ github.run_id }}
        run: scripts/run_marley.sh

      - name: Summarize the event
        run: |
          python3 scripts/summarize_event.py events.hepmc3 --text
          {
            echo '### MARLEY event'
            echo '```'
            python3 scripts/summarize_event.py events.hepmc3 --text
            echo '```'
          } >> "$GITHUB_STEP_SUMMARY"

      - name: Upload event files
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: marley-event-${{ github.run_id }}
          path: |
            events.hepmc3
            events_legacy.txt
            one_event.seed*.js
          if-no-files-found: warn

      # Published for the Garmin app to read. Written even on failure so the
      # watch can show "failure" instead of a stale event.
      - name: Build the published event file
        if: always()
        env:
          STATUS: ${{ job.status }}
          INVOKER: ${{ inputs.invoker }}
          SEED: ${{ github.run_id }}
          RUN_URL: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
        run: |
          mkdir -p _site
          scripts/event_payload.sh events.hepmc3 | tee _site/latest_event.json

      - name: Upload the Pages artifact
        if: always()
        uses: actions/upload-pages-artifact@v3
        with:
          path: _site

      # Runs even on failure so a broken build is reported in Slack rather than
      # silently leaving the request unanswered.
      - name: Announce in Slack
        if: always()
        env:
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
          STATUS: ${{ job.status }}
          INVOKER: ${{ inputs.invoker }}
          SEED: ${{ github.run_id }}
          RUN_URL: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
        run: |
          # Built by scripts/event_payload.sh so the identical payload can be
          # produced locally while iterating on the message (see slack/SETUP.md).
          # Missing events.hepmc3 is fine: every field then reports "n/a".
          scripts/event_payload.sh events.hepmc3 > payload.json
          echo "Payload:"
          cat payload.json

          if [ -z "$SLACK_WEBHOOK_URL" ]; then
            echo "SLACK_WEBHOOK_URL is not set -- skipping the Slack post."
            exit 0
          fi

          curl --fail-with-body -sS -X POST \
            -H 'Content-Type: application/json' \
            --data @payload.json \
            "$SLACK_WEBHOOK_URL"

  publish:
    # always() so a failed generation still publishes its "failure" payload.
    if: always()
    needs: generate
    runs-on: ubuntu-latest
    permissions:
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    # Pages allows one concurrent deployment; keep it separate from the
    # marley-generate group so a queued build cannot block a deploy.
    concurrency:
      group: pages
      cancel-in-progress: false
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
